<div class="wrap">
  <div class="pulse"></div>
  <div class="ring"></div>

  <div class="text">
    <div class="line">Breathe in&hellip;</div>
    <div class="line">Breathe out&hellip;</div>
  </div>
</div>

<style>
.wrap {
  display: grid;
  place-items: center;   /* centers children */
  width: clamp(220px, 28vw, 360px);
  aspect-ratio: 1 / 1;
  position: relative;
}

.pulse, .ring {
  position: absolute;
  inset: 0;
  border-radius: 50%;
}

.pulse {
  background: radial-gradient(closest-side, var(--c1) 0 55%, transparent 56%);
  animation: breathe 6s ease-in-out infinite;
}

.ring {
  border: 2px solid color-mix(in oklab, var(--c1), white 40%);
  animation: ring 6s ease-in-out infinite;
}

.text {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;   /* 👈 puts text in the exact middle */
  font-weight: 600;
  color: var(--ink);
  text-align: center;
}

.line {
  position: absolute;
  opacity: 0;
  animation: say 6s infinite;
}
.line:nth-child(1) { animation-delay: 0s; }
.line:nth-child(2) { animation-delay: 3s; }

@keyframes breathe {
  0%,100% { transform: scale(.92); }
  50%     { transform: scale(1.12); }
}
@keyframes ring {
  0%,100% { transform: scale(1); opacity: .8; }
  50%     { transform: scale(1.08); opacity: 1; }
}
@keyframes say {
  0%,40%   { opacity: 1; }
  50%,100% { opacity: 0; }
}

:root {
  --c1: oklch(78% 0.09 220);
  --ink: oklch(25% 0.02 250);
}
</style>